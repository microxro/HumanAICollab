# Installed by `npm install` — reproducible from package.json + the lockfile.
node_modules/

# Netlify build/dev artifacts
.netlify/

# Local repo knowledge graph (Claude Code /graph skill) — regenerable, not source.
.graph/

# Local environment overrides — never commit real keys.
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
